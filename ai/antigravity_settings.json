{
  "editor": "vim",
  "enableTerminalSandbox": true,
  "model": "Gemini 3.5 Flash (Medium)",
  "permissions": {
    "allow": [
      "command(find)",
      "command(git log)",
      "command(git show)",
      "command(cat)",
      "command(git status)",
      "command(fd)",
      "command(rg)",
      "command(jq)",
      "command(uv)",
      "command(ruff)",
      "command(bat)"
    ]
  }
}
